# 项目整体要求

* 本项目中所有需要显示时间或格式化时间的地方，都使用本地时间。
* 每次需求变动结束，都要考虑是否需要添加或修改测试用例。需要的话，请添加或修改测试用例。
* 每次更新PRD.md文件时，都要更新版本号，并在代码中同步该版本号。
* 在调用相关命令进行测试或编译时，请避免使用head, tail, pipeline, &&, ||, grep 等不支持的命令，本机环境为Windows，使用PowerShell。
* 项目调试过程中，有可能为了方便调试而添加一些临时代码，在这些临时代码的前后做上明显的标记（如注释），方便后续的删除。
* 调试相关功能在正常环境下可见可调。不要使用开发环境。
* 如要在界面上打印调试信息到窗口，请在调试窗口中提供复制按钮，方便复制调试信息。