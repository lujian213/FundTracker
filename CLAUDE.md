# 项目整体要求

## 整体要求
* 禁止使用worktree
* 不要执行git命令添加文件或提交代码。
* 所有回答使用中文。
* 本项目中所有需要显示时间或格式化时间的地方，都使用本地时间（用户浏览器的时间）。不要写死任何固定时区，比如中国时区。
* 每次需求变动结束，都要考虑是否需要添加或修改测试用例。需要的话，请添加或修改测试用例。修改或添加测试用例时，请以表格形式列出修改或添加的测试点。
* 没有用户指令，不要修改文档。你自己创建的设计文档文档等不在此列。
* 每次更新PRD.md文件时，都要更新版本号，并在代码中同步该版本号（version.ts）。但需要我的显示指令来决定是否提升版本号。
* 合并新的需求时，注意不要在PRD.md中删除原有内容（除非有明确指令说原有需求被删除），但可以做补充和修正。

## 测试编写原则
* 如果测试难写，先重构代码，不要硬写复杂测试。难以测试的代码往往意味着设计需要改进。
* 测试应聚焦业务逻辑，mock 实现细节（队列、网络、定时器等）是方向错误。
* 优先提取纯函数进行测试，纯函数无需 mock 即可独立验证。

## 编辑文件注意事项
* 编辑代码时，注意不要在行头遗留“+”，“-”等版本控制系统的标记。以及diff带来的文件冲突标记（如<<<<<<<, =======, >>>>>>>等）。如果有，请删除这些标记。
* 每次代码编辑结束，主动进行编译，尽早发现其中的编译/语法错误等。使用npm run build命令进行编译。如获取输出失败，第二次尝试可以将输出重定向到文件中（如npm run build > output.txt 2>&1），然后查看output.txt中的错误信息。

## 调试要求
* 项目调试过程中，有可能为了方便调试而添加一些临时代码，在这些临时代码的前后做上明显的标记（如注释），方便后续的删除。
* 调试相关功能在正常环境下可见可调。不要使用开发环境。
* 如要在界面上打印调试信息到窗口，请在调试窗口中提供滚动条和复制按钮，方便复制调试信息。
* 调试代码必须在调试结束后立即移除，不得留存在最终代码中。但必须等待我的显示要求后和确认后再移除，不要提前移除。
* 为了便于快速识别和清理调试代码，请按以下规范添加标记：
  - 使用 `// DEBUG_START` 和 `// DEBUG_END` 包围调试代码块
  - 或使用 `// @DEBUG @TEMP:` 注释标记单行调试代码
  - 调试标记中应包含目的说明和日期
  - 可通过 `grep -r "@DEBUG\|DEBUG_START\|DEBUG_END" .` 快速定位所有调试代码
* 当调试结束时，用户要求删除调试代码时，可以尝试对文件进行git diff，如果发现所有的改动都是本次调试引入的调试代码，那么可以直接用git中的最新版本覆盖当前文件，来快速删除调试代码。前提是当前文件中没有其他改动（如需求变动引入的改动）。如果当前文件中有其他改动，那么就需要像原来一样的逐步删除调试代码了。