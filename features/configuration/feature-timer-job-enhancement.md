# Timer Job Enhancement
对目前系统中的后台定时任务进行统一的调度和管理。

## 配置集中化
把所有的后台定时任务的定时配置集中到一个配置文件里进行管理，方便后续的修改和扩展。配置文件里每个定时任务的配置项包括：
* 任务名称：定时任务的名字，用户可以自行输入。
* 定时规则：定时任务的执行规则，可以使用cron表达式来定义。

定时任务的执行逻辑由一个统一的调度器来管理，调度器会根据配置文件里的定时规则，定时触发对应的任务执行。

